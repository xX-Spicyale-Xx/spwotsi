{
  "name": "website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "npm install --legacy-peer-deps && vercel build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.10.0"
  },
  "devDependencies": {
    "@astrojs/image": "^0.18.0"
  }
}
